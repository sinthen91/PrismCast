GitHub Actions release automation will be added after the first local PrismCast build succeeds.

The first alpha deliberately keeps release publishing manual so a bad CI assumption does not turn every tag into a broken Dalamud package.
